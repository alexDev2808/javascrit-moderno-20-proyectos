// Orden de ejecucion

let resultFinal = 0;


resultFinal = 20 + 32 - 4;
console.log(resultFinal);

// Para operaciones tiene mas prioridad +- unario, (), */%, +- binario
// Para expresiones con el mismo nivel de prioridad, existe el enlace hacia la izquierda, se resuelven primero las operaciones de la izquierda
resultFinal = 45 * 2 / 3;
console.log(resultFinal);
